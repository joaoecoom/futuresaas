export default function TrustBadges() {
  return (
    <div className="checkoutTrustBadges" role="group" aria-label="Segurança do pagamento">
      <span className="checkoutTrustBadge">SSL</span>
      <span className="checkoutTrustBadge">Stripe</span>
      <span className="checkoutTrustBadge">Visa / Mastercard</span>
    </div>
  );
}
